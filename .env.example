# Copy this file to ".env" (which is git-ignored) and fill in your values.
# Used by scripts/seed-aircraft-images.mjs to seed the aircraft image index.
#
# SUPABASE_SERVICE_KEY is a SECRET: it bypasses Row-Level Security. It is only used
# locally by the seeder. NEVER commit it, paste it into chat, or share it.

SUPABASE_URL=https://ewmjygogceutvgalnlns.supabase.co
SUPABASE_SERVICE_KEY=your-service_role-key-here
